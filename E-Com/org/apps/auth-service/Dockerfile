# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @org/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t org-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system org-auth-service && \
          adduser --system -G org-auth-service org-auth-service

COPY apps/auth-service/dist org-auth-service/
COPY apps/auth-service/package.json org-auth-service/
RUN chown -R org-auth-service:org-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix org-auth-service --omit=dev -f install

CMD [ "node", "org-auth-service" ]
